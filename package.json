{
  "name": "endless-runner",
  "version": "0.0.0",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "start": "node main.js",
    "postinstall": "vite build",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^2.7.0"
  },
  "dependencies": {
    "gh-pages": "^3.2.3",
    "three": "^0.135.0"
  }
}
